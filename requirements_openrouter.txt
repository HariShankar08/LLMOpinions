# Core dependencies for OpenRouter integration
requests>=2.25.0
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0
tqdm>=4.62.0

# Optional HuggingFace dependencies (for local models)
transformers>=4.20.0
torch>=1.12.0
tokenizers>=0.12.0
huggingface-hub>=0.8.0

# Optional dependencies for paraphrasing (legacy approach)
# parrot-paraphraser>=1.0.0  # Uncomment if needed

# For development and testing
python-dotenv>=0.19.0
